# Supabase — create a free project at https://supabase.com
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Server-only secret (never expose to the browser)
SUPABASE_SERVICE_ROLE_KEY=

# Public base URL of the deployed app (used in QR verification links)
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Brevo transactional email (optional — decision emails no-op if unset)
BREVO_API_KEY=
EMAIL_FROM=
